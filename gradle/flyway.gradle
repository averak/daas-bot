flyway {
    driver = BASE_PROPERTY.spring.datasource.driver
    url = BASE_PROPERTY.spring.datasource.url
    user = BASE_PROPERTY.spring.datasource.username
    password = BASE_PROPERTY.spring.datasource.password
}
